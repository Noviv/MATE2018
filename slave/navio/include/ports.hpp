#ifndef PORTS_H
#define PORTS_H

#include <string>
#include <unordered_map>

enum Port {
    FTL = -1,
    FBL = -1,
    FTR = -1,
    FBR = -1,
    RTL = -1,
    RBL = 5,
    RTR = -1,
    RBR = -1
};

#endif
